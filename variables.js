// hacer un que diga que un numero es par o impar
function num (num) {
    if (num/2*2==num){ 
        return ("el numero es par");
    } else 
    return ("el numero es impar");
    
 }
console.log(17);